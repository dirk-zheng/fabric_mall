import { useMemo, useState } from 'react';
import { Search, SlidersHorizontal, Download } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useStore } from '../context/StoreContext';
import { categories } from '../data/products';
import ProductCard from '../components/ProductCard';

export default function Products(){const {state}=useStore();const [category,setCategory]=useState('all');const [query,setQuery]=useState('');const products=useMemo(()=>state.products.filter(p=>(category==='all'||p.category===category)&&`${p.name} ${p.description} ${p.wash||''} ${(p.specs||[]).join(' ')}`.toLowerCase().includes(query.toLowerCase())),[state.products,category,query]);return <div className="min-h-screen bg-[#fbf8f2] pt-24"><section className="bg-[#dfe6df] px-5 py-16 text-center"><p className="text-sm font-bold uppercase tracking-[.22em] text-[#a84d33]">Denim fabric library · Mill-direct B2B supply</p><h1 className="mt-3 font-heading text-5xl font-bold text-[#17251f]">Fabric selected by what you need it to do.</h1><p className="mx-auto mt-4 max-w-3xl text-[#58655e]">Explore rigid, stretch, lightweight and specialty denim qualities. Technical sheets, swatches, testing and bulk terms are confirmed for each inquiry.</p><Link to="/contact" className="mt-7 inline-flex items-center gap-2 rounded-full bg-[#17251f] px-6 py-3 font-semibold text-white"><Download size={17}/> Request swatches &amp; fabric book</Link></section><main className="mx-auto max-w-7xl px-5 py-12 sm:px-8"><div className="flex flex-col gap-5 border-b border-[#17251f]/10 pb-7 lg:flex-row lg:items-center lg:justify-between"><div className="flex flex-wrap gap-2">{categories.map(c=><button key={c.id} onClick={()=>setCategory(c.id)} className={`rounded-full px-5 py-2.5 text-sm font-semibold ${category===c.id?'bg-[#17251f] text-white':'border border-[#17251f]/15 bg-white text-[#334139]'}`}>{c.name}</button>)}</div><label className="relative block min-w-[280px]"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-[#657068]" size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search weight, fiber or finish" className="w-full rounded-full border-[#17251f]/15 bg-white py-3 pl-11 pr-4"/></label></div><div className="my-8 flex items-center justify-between"><p className="text-sm text-[#657068]">{products.length} fabric qualities</p><span className="flex items-center gap-2 text-sm font-semibold"><SlidersHorizontal size={16}/> MOQ by quality &amp; color</span></div>{products.length?<div className="grid gap-x-6 gap-y-12 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="py-24 text-center"><h2 className="font-heading text-2xl font-bold">No fabrics found</h2><p className="mt-2 text-[#657068]">Try another weight, finish or category.</p></div>}<section className="mt-16 rounded-[2rem] bg-[#17251f] p-8 text-white sm:flex sm:items-center sm:justify-between"><div><h2 className="font-heading text-2xl font-bold">Need a quality outside the core library?</h2><p className="mt-2 text-white/60">Share a reference swatch or your composition, weight, stretch, shade and performance targets.</p></div><Link to="/contact" className="mt-5 inline-block rounded-full bg-white px-6 py-3 font-semibold text-[#17251f] sm:mt-0">Send a fabric brief</Link></section></main></div>}
