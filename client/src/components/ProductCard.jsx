import { Link } from 'react-router-dom';
import { ArrowUpRight, FileText } from 'lucide-react';
import { productSlug } from '../data/seoContent';
import { categoryNames } from '../data/products';

export default function ProductCard({product}){return <article className="group"><Link to={`/products/${productSlug(product)}`}><div className="relative overflow-hidden rounded-[1.75rem] bg-[#eee9e0]"><img src={product.image} alt={product.name} className="aspect-[4/3] w-full object-cover transition duration-500 group-hover:scale-105" onError={e=>{e.currentTarget.src='/no-image.png'}}/><span className="absolute left-3 top-3 rounded-full bg-white/95 px-3 py-1 text-xs font-bold text-[#a84d33]">{product.badge||categoryNames[product.category]}</span><span className="absolute bottom-3 right-3 grid h-10 w-10 place-items-center rounded-full bg-[#17251f] text-white opacity-0 transition group-hover:opacity-100"><ArrowUpRight size={18}/></span></div><div className="mt-4"><p className="text-xs font-bold uppercase tracking-wider text-[#a84d33]">{categoryNames[product.category]}</p><h2 className="mt-1 font-heading text-xl font-semibold text-[#17251f]">{product.name}</h2><p className="mt-2 text-sm text-[#657068]">{product.sizes||'Weight by quality'} · {product.wash||'Indigo'}</p><div className="mt-3 flex items-center justify-between border-t border-[#17251f]/10 pt-3 text-xs"><span><strong>MOQ:</strong> {product.moq||'Confirmed by quality'}</span><span className="flex items-center gap-1 font-bold text-[#a84d33]"><FileText size={13}/> Request swatch</span></div></div></Link></article>}
